// Changelog story — the component's HISTORY lives here (not in JSDoc, inline comments,
// or spec.yaml). One Story, newest-first, one row per change. A brand-new component
// ships a single "v1.0.0 — initial" row.
//
// Drop this into <Name>.stories.tsx as the third (required) story, alongside
// Playground (required) + AllVariants (optional). Max 3 stories — never more.
// Per STORY_FORMAT.yaml.story_file.required_stories.required_third_story.

export const Changelog: Story = {
  // No args + no controls: this story is a static history table, not an interactive demo.
  parameters: { controls: { disable: true } },
  render: () => (
    <dl className="grid grid-cols-[auto_1fr] gap-x-4 gap-y-1 text-sm">
      <dt className="font-mono text-[var(--color-muted-foreground)]">1.0.0 — YYYY-MM-DD</dt>
      <dd>initial</dd>
      {/* newest entries go ABOVE older ones */}
    </dl>
  ),
};
