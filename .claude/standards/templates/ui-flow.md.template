# UI design — <flow-name>

> Replace `<flow-name>` with the flow name (matches `__specs__/flows/<flow-name>.flow.yaml`).
> Replace every `<PLACEHOLDER>` below. Delete any section that genuinely does not apply with a one-line "n/a — <reason>".
> The `<!-- ui-locked: YYYY-MM-DD -->` marker at the end is inserted by the USER when they approve the design.
> Spec-writer NEVER inserts the lock marker — only the user does.

## Wireframe

```
+--------------------------------------------------+
|  <ASCII or unicode-box sketch of the screen at  |
|   entry state — show layout, primary controls,  |
|   text positions. Keep it crude; this is for    |
|   alignment on structure, not pixel-perfect.>   |
+--------------------------------------------------+
```

## States

Every distinct visual state the user can land on. One per line.

1. **loading** — <one-line description>
2. **empty** — <one-line description>
3. **populated** — <one-line description>
4. **submitting** — <one-line description>
5. **success** — <one-line description>
6. **error** — <one-line description; reference error mode from flow.yaml>

## Interactions

What happens on each user action. Reference the flow.yaml's `paths` keys where relevant.

1. **<action name>** — e.g., "Submit button click" → calls flow `<flowName>` happy path; on success transitions to state `success`.
2. **<action name>** — e.g., "Cancel link click" → navigates to `/<route>`.
3. **<action name>** — e.g., "Field blur on email" → runs validation; on fail transitions to state `error` with message `<msg>`.

## Components

Every shadcn/ui (or equivalent) component used. One per line.

1. `<Button>` from shadcn/ui
2. `<Input>` from shadcn/ui
3. `<Card>` from shadcn/ui
4. `<Alert>` from shadcn/ui
5. <custom component path, e.g., `src/features/<feature>/components/<Name>.tsx`>

## Storybook

Planned story file: `src/features/<feature>/<flow-name>.stories.tsx`

Stories: one per state listed above (`loading`, `empty`, `populated`, `submitting`, `success`, `error`).

## Agent operability

Every interactive element carries:
- `data-testid="<feature>-<flow>-<element>"`
- `data-agent-action="<verb>"` (e.g., `submit`, `cancel`, `dismiss`)
- `data-agent-step="<step-number-in-happy-path>"`
- `aria-label="<plain-english>"`

## Open questions

(Optional — list anything the user must decide before lock.)

1. <question>
2. <question>

---

<!--
When you (the user) approve this design, append a single line at the very end:

    <!-- ui-locked: 2026-MM-DD -->

(Using today's date in YYYY-MM-DD format. No spaces inside the angle-brackets other than as shown.)

The verify-ui-design-locked.mjs gate will then permit the coder phase to start.
The spec-writer agent NEVER inserts this line itself.
-->
