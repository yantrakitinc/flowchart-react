# <ComponentName> — research

> Phase 0 artifact across atomic / composite / page tiers.
> Lives at `<component-folder>/__specs__/research.md`.
> Depth derives from COMPONENT_CREATION.yaml.coverage_x_stakes_matrix: light | medium | thorough.
> Fill every `<PLACEHOLDER>`. Delete any section that genuinely doesn't apply with a one-line `n/a — <reason>` (keep the heading so the verifier sees it).
> `design.md` MUST reference this file; `verify-component-research-cited.mjs` enforces.

## Classification

- **Tier**: <atomic | composite | page>
- **Library coverage**: <full | partial | none> — <one-line reasoning>
- **Stakes**: <internal | standard | brand> — <how derived: autonomy.yaml.brand_surfaces match, folder inference, or in-chat declaration>
- **Resulting depth**: <light | medium | thorough> (from the matrix)
- **Domain survey trigger**: <skip | brand-language-only | targeted-gaps | mandatory-full> — <one-line reasoning>

## Competitor scan

Document how shadcn / MUI / DaisyUI implement this component. Cite URLs.

### shadcn/ui

- **Doc**: <URL or "n/a — no shadcn primitive">
- **API shape**: <props, variants, asChild support>
- **A11y notes from docs**: <what the shadcn docs say about a11y>
- **Variants shipped**: <list>
- **Strengths**: <one-line>
- **Gaps for our use**: <one-line; or "none">

### MUI (Material UI)

- **Doc**: <URL or "n/a">
- **API shape**: <props, variants, sx prop>
- **A11y notes**: <what MUI's docs and source claim>
- **Variants shipped**: <list>
- **Strengths**: <one-line>
- **Gaps for our use**: <one-line>

### DaisyUI

- **Doc**: <URL or "n/a">
- **API shape**: <class-name composition; no React API>
- **A11y notes**: <what DaisyUI claims; usually inherits from native HTML>
- **Variants shipped**: <list>
- **Strengths**: <one-line>
- **Gaps for our use**: <one-line>

## Baseline decision

- **Choice**: <wrap shadcn baseline | wrap MUI | wrap DaisyUI patterns | custom-justified>
- **Reasoning**: <2-4 sentences referencing the comparison above>
- **API shape we adopt**: <MUI | DaisyUI | shadcn | custom — match COMPONENT_CREATION.yaml.api_shape>
- **Why the unchosen options were rejected**: <one-line each>

If **custom-justified**: name which baseline was closest and why it doesn't fit. The verifier accepts the choice; reviewer scrutinizes.

## Domain pattern survey

> Required when library_coverage == none, when tier in [composite, page], or when stakes == brand AND a domain pattern is involved. Otherwise mark "skip — <reason>" and continue.

### Industry leaders surveyed

For each domain leader, name what they do that informs our design. Include URLs.

| Source | Pattern observed | URL | Adopted? |
|---|---|---|---|
| <e.g., Discourse> | <thread display pattern> | <URL> | <yes/partial/no — reason> |
| <e.g., Slack> | <chat composition pattern> | <URL> | <yes/partial/no — reason> |
| <e.g., MUI X DataGrid> | <virtual scrolling pattern> | <URL> | <yes/partial/no — reason> |

### Patterns converged across leaders

What multiple leaders do the same way (likely user expectation):

1. <pattern>: <one-line description>
2. <pattern>: <one-line description>

### Patterns where leaders diverge

What leaders do differently (signals a real design choice for us):

1. <pattern>: <leader A> does <X>, <leader B> does <Y>. We choose <X|Y|hybrid> because <reason>.

### Primary sources cited

References to spec / standards documents:

- WAI-ARIA APG pattern: <URL or "n/a">
- WCAG criteria invoked: <SC numbers + URL or "n/a">
- WHATWG HTML semantics: <URL or "n/a">
- Browser implementation references: <Chromium DevTools docs, Firefox a11y inspector, etc., or "n/a">

## Recommendation

The synthesis the design phase consumes. Be concrete — name props, variants, behaviors, edge cases.

1. **Baseline**: <shadcn Button New York style | MUI Button v6 props | custom>
2. **API shape**: <inline summary of variants × colors × sizes × extras to expose at v1>
3. **A11y pattern**: <WAI-ARIA APG URL + which aspects we adopt>
4. **State vocabulary**: <which shared `state-*` classes the component will consume>
5. **Tokens**: <which CSS variables the component will read>
6. **Edge cases worth highlighting in design.md**: <list>
7. **Deferred for follow-ups**: <variants/sizes/colors NOT shipped at v1 + why>

## Confidence + open questions

- **Confidence in this recommendation**: <high | medium | low> — <one-line on what could change it>
- **Open questions for the user**: <list any decisions the agent surfaces; absent here = no user input needed for design phase>

---

<!-- research-phase-complete -->
