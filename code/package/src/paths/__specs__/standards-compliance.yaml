status: unlocked
verified: "100%"
last_validated: "PENDING - verifier Mode A must run the gates and stamp status:locked (spec-writer does not self-stamp the lock; see LOCK_FILES.md stamping_authority)"
feature: src/paths

standards:
  AUTHORIZATION_STANDARDS: "NOT REQUIRED — no DB access, no principal, pure computation"
  COMPONENT_CREATION: "NOT REQUIRED — no UI atomic component authored"
  COMPOSITE_CREATION: "NOT REQUIRED — no UI composite authored"
  PAGE_CREATION: "NOT REQUIRED — no UI page/route/surface"
  E2E_TESTING: "NOT REQUIRED — no HTTP/browser transport surface to drive E2E; behavior is fully covered by unit tests"
  GOOGLE_FIREBASE_STANDARDS: "NOT REQUIRED — no Firebase/Firestore/Storage/Auth usage"
  INDUSTRY_STANDARDS_STACK: "NOT REQUIRED — no HTTP/auth/crypto protocol surface"
  PACKAGE_PROJECT_STANDARDS: "100% — module ships inside the published @yantrakit/flowchart-react package via the index.ts barrel export contract"
  PROCESS_DISCIPLINE: "100% — scope discipline, defect-surfacing, and foundational ordering followed in this backfill"
  PROJECT_ARCHITECTURE: "NOT REQUIRED — not a Next.js app; no DB/SSR/CUID2 concerns"
  SITE_BLUEPRINT: "NOT REQUIRED — no page-tier UI surface"
  STANDARDS_CREATION_STANDARDS: "NOT REQUIRED — not authoring a standards doc"
  SUPERPOWERS: "NOT REQUIRED — no slash-command/skill authored by this feature"
  TEST_NAMING: "100% — detectPaths.test.ts follows the stable test-id/discovery convention"
  VERCEL_STANDARDS: "NOT REQUIRED — published npm package, not a Vercel-deployed app"
  ACCESSIBILITY: "NOT REQUIRED — no UI rendered by this module"
  AGENT_AFFORDANCES: "NOT REQUIRED — no HTTP/UI surface (data-testid/agents.json n/a); agent-callability covered instead by flow.yaml's ai_agent_action block"
  API_SURFACE: "NOT REQUIRED — no HTTP/server-action surface"
  BRANCHES_AND_COMMITS: "100% — this slice's commits follow the repo's branch/commit convention"
  CODE_DOCUMENTATION: "100% — JSDoc present on every export (module doc + iPathType + iFlowPath + iPathDetectionResult + detectPaths)"
  COMPONENT_DOCS: "NOT REQUIRED — not a UI component"
  COMPONENT_FOLDERS: "NOT REQUIRED — not a UI component folder"
  COMPONENT_LIBRARY_DOCTRINE: "NOT REQUIRED — no UI component"
  DESIGN_TOKENS: "NOT REQUIRED — no styling/CSS in this module"
  ENV_VARS: "NOT REQUIRED — no environment variables consumed"
  FLOW_CONTRACT: "100% — flows/detect-paths.flow.yaml ships the full schema with exhaustive happy/edge_boundary_* enumeration plus a mermaid diagram"
  USER_JOURNEYS: "NOT REQUIRED — internal leaf computation with no standalone user journey; consumed by host-app journeys which own their own journey catalog"
  LABS: "NOT REQUIRED — no visual sandbox surface shipped by this module"
  HEXAGONAL_ARCHITECTURE: "NOT REQUIRED — pure computation, no ports/adapters/DI/repository to structure"
  I18N: "100% — returns structural node/edge ids and the closed iPathType enum only; zero hardcoded user-facing strings"
  ISSUES: "NOT REQUIRED — no GitHub issue artifact lives in this folder; governs the surrounding issue workflow, not the spec contract"
  LOCAL_DEV_NETWORK: "NOT REQUIRED — no dev server/port for a pure library module"
  LOCK_FILES: "100% — this file follows the standards-compliance.yaml schema"
  LOGGING: "NOT REQUIRED — no logging call sites; pure computation, no runtime logger"
  MANUAL_FLOWS: "NOT REQUIRED — no HTTP/CLI/UI surface exists in this pure-TS module; manual/detect-paths.md is provided anyway (adapted, no results-posting route) per SPEC_CONTRACT's optional manual/ subfolder"
  MOBILE_FIRST: "NOT REQUIRED — no UI surface"
  MOCKING_DOCTRINE: "NOT REQUIRED — zero external dependencies to mock; pure function tested directly with real inputs"
  NAMING: "100% — i-prefix types (iPathType/iFlowPath/iPathDetectionResult), ALL_CAPS constants (MAX_PATHS/MAX_DEPTH), camelCase file name"
  NO_THEATER_TESTS: "100% — every test asserts real computed output (path counts/types/ids) against real inputs; no mocked-everything theater"
  PULL_REQUESTS: "NOT REQUIRED — governs the PR mechanics shipping this slice, not the spec artifacts themselves"
  README_CONTRACT: "NOT REQUIRED — governs repo-root README.yaml, not this feature folder"
  REPO_GATE_INSTALLATION: "NOT REQUIRED — repo-level hook/installer mechanics, not per-feature"
  REPO_PROVISIONING: "NOT REQUIRED — repo/board provisioning, not per-feature"
  ROOT_LAYOUT: "NOT REQUIRED — repo-root folder contract, not per-feature"
  SCENARIO_ENUMERATION: "100% — flows/detect-paths.flow.yaml's edge_boundary_* set walks every scenario this pure function's code paths present"
  SOURCE_COVERAGE: "100% — detectPaths.ts and index.ts both resolve to this spec"
  SOURCE_FOLDERS: "100% — src/paths/ follows the per-feature sub-folder vocabulary, self-contained, no padding folders"
  SPEC_CONTRACT: "100% — this __specs__/ folder follows the required layout (spec.yaml + spec.md + standards-compliance.yaml + flows/ + manual/)"
  STACK: "NOT REQUIRED — repo-wide framework baseline already fixed at repo level (React library, tsup, Vitest); nothing per-feature to declare"
  STATE_VOCABULARY: "NOT REQUIRED — no UI state classes"
  STORYBOOK_SETUP: "NOT REQUIRED — no Storybook story for a pure non-UI function"
  STORYBOOK_TESTING: "NOT REQUIRED — no Storybook coverage applicable to a non-UI function"
  STORY_FORMAT: "NOT REQUIRED — no Storybook story"
  TEST_STACK: "100% — Vitest is the correct tool for this pure-TS unit suite, per the one-tool-per-job rule"
  TYPESCRIPT_HYGIENE: "100% — strict mode, no any/ts-ignore, types co-located and exported cleanly via index.ts"
  UNIT_COVERAGE: "100% — 100% line/branch/function/statement coverage; every export tested; red/green TDD"
  UTC_TIMESTAMPS: "NOT REQUIRED — no timestamps produced/stored by this module"
  VERIFIER_MODES: "NOT REQUIRED — spec-writer output does not itself execute or assert a verifier mode; the verifier applies its own mode catalog to this feature"
  VERIFY_MANUAL_STORIES: "NOT REQUIRED — no per-component Verify-Manual story; not a UI component"
  WRITING_ORDER: "100% — this is a sanctioned legacy-backfill (feature-spec-writer-core Mode 3); spec written from the pre-existing shipped source without modifying it; verifier Mode A still owns the lock"
  DECISION_LOG: "NOT REQUIRED — no ambiguous user ruling arose during this backfill"
  BROWSER_VALIDATION: "NOT REQUIRED — no UI is touched by this module; nothing for the Chrome-extension walker to drive"
  API_ENVELOPE: "NOT REQUIRED — no HTTP API response envelope produced by this module"
  API_FIRST: "NOT REQUIRED — no HTTP API surface shipped by this module"
  REQUIREMENTS_CONTRACT: "NOT REQUIRED — legacy backfill of pre-existing shipped behavior, not a net-new requirements-gathering cycle"
  SELF_HARDENING: "NOT REQUIRED — no learned-failure/lesson filed against this feature during this backfill"
  CONTEXT_ECONOMY: "100% — this dispatch narrowed reads to the targeted source/standards files rather than pulling the whole repo into context"
