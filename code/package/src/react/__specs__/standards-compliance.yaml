status: unlocked
verified: "100%"
last_validated: 2026-07-26T00:00:00Z
feature: src/react

standards:
  AUTHORIZATION_STANDARDS: "NOT REQUIRED — no DB, no permission-gated operation; pure client-only rendering component"
  COMPONENT_CREATION: "100%"
  COMPOSITE_CREATION: "NOT REQUIRED — this package IS the primitive/library tier itself (analogous to a shadcn-tier package), not an app consuming a shared atomic set into a feature-local composite"
  PAGE_CREATION: "NOT REQUIRED — no app pages/routes/site surfaces; this repo is a published component library, not a Next.js app"
  E2E_TESTING: "NOT REQUIRED — no HTTP/DB backend to E2E; user-facing behavior is fully exercised via Vitest + Testing Library component/interaction tests (the correct tool for a client-only UI library per TEST_STACK)"
  GOOGLE_FIREBASE_STANDARDS: "NOT REQUIRED — no Firebase usage"
  INDUSTRY_STANDARDS_STACK: "NOT REQUIRED — none of the named industry protocols (OAuth/OIDC/WebAuthn/etc.) apply to a client-only rendering library"
  PACKAGE_PROJECT_STANDARDS: "100%"
  PROCESS_DISCIPLINE: "100%"
  PROJECT_ARCHITECTURE: "NOT REQUIRED — describes a single Next.js app's server/client layering; this repo is a standalone client-only npm package with no server"
  SITE_BLUEPRINT: "NOT REQUIRED — no site/pages in a component-library repo"
  STANDARDS_CREATION_STANDARDS: "NOT REQUIRED — governs authoring of standards docs themselves, not feature code"
  SUPERPOWERS: "NOT REQUIRED — governs the superpowers plugin workflow, not this feature's artifacts"
  TEST_NAMING: "100%"
  VERCEL_STANDARDS: "NOT REQUIRED — this package is npm-published, never deployed to Vercel"
  ACCESSIBILITY: "100%"
  AGENT_AFFORDANCES: "NOT REQUIRED — GAP, not an inapplicability: data-testid is required on every interactive element by this standard and is verified ABSENT throughout src/react/ (FlowNode/PathDrawer/FlowChart carry data-agent-action + data-node-id/data-path-id + aria-label but zero data-testid attributes). Recorded here as a placeholder pending a coder slice to add the attribute — see the dispatch's end-of-turn report for the full finding; do NOT read this as certifying compliance."
  API_SURFACE: "NOT REQUIRED — no HTTP routes/server actions"
  BRANCHES_AND_COMMITS: "NOT REQUIRED — this dispatch produced spec files only; no branch/commit created by this agent"
  CODE_DOCUMENTATION: "100%"
  COMPONENT_DOCS: "NOT REQUIRED — GAP, not an inapplicability: no *.stories.tsx / readme.mdx / usage.mdx exist yet for FlowChart/PathDrawer/FlowNode; Storybook tooling is wired at the repo level (.storybook/) but no per-component doc entries exist — flagged as an open backfill item in the end-of-turn report"
  COMPONENT_FOLDERS: "NOT REQUIRED — governs the Next.js in-app component tree (src/components/ui, src/features/); this repo is a standalone package with its own self-contained src/react/ layout (SOURCE_FOLDERS governs that instead)"
  COMPONENT_LIBRARY_DOCTRINE: "100%"
  DESIGN_TOKENS: "100%"
  ENV_VARS: "NOT REQUIRED — no env vars consumed by this feature at runtime"
  FLOW_CONTRACT: "100%"
  USER_JOURNEYS: "NOT REQUIRED — journey catalog scope is product-level end-user SaaS journeys; this is a building-block component library with no journeys of its own (host apps own their journeys)"
  LABS: "NOT REQUIRED — no /labs surface in this package repo"
  HEXAGONAL_ARCHITECTURE: "100%"
  I18N: "NOT REQUIRED — every end-user-visible diagram string is caller-supplied and inherits the host's own i18n layer; the 3 package-authored chrome strings ('Laying out…', 'Flowchart parse error', 'All paths') are developer-facing rendering-library chrome, not localizable product copy — self-derived judgment call, open to override (see report)"
  ISSUES: "NOT REQUIRED — no GH issue created by this dispatch; process standard, not a feature artifact"
  LOCAL_DEV_NETWORK: "NOT REQUIRED — no dev server/ports owned by this feature"
  LOCK_FILES: "100%"
  LOGGING: "NOT REQUIRED — no logging/audit calls in a pure rendering component"
  MANUAL_FLOWS: "100%"
  MOBILE_FIRST: "100%"
  MOCKING_DOCTRINE: "NOT REQUIRED — no external I/O to mock; tests render real components against real in-memory IR graphs"
  NAMING: "100%"
  NO_THEATER_TESTS: "100%"
  PULL_REQUESTS: "NOT REQUIRED — no PR opened by this dispatch"
  README_CONTRACT: "NOT REQUIRED — repo-root binding concern (README.yaml), not a per-feature artifact"
  REPO_GATE_INSTALLATION: "NOT REQUIRED — repo-level hook/CI installation mechanics, not a feature artifact"
  REPO_PROVISIONING: "NOT REQUIRED — repo/project-board provisioning, not a feature artifact"
  ROOT_LAYOUT: "NOT REQUIRED — repo-root folder contract, not a per-feature artifact"
  SCENARIO_ENUMERATION: "100%"
  SOURCE_COVERAGE: "100%"
  SOURCE_FOLDERS: "100%"
  SPEC_CONTRACT: "100% — note: test files are co-located as `*.test.tsx` siblings rather than nested under a `__tests__/` subfolder (pre-existing repo convention, not introduced by this spec) — see end-of-turn report"
  STACK: "NOT REQUIRED — STACK.md's Next.js-first baseline is documented-overridden here: this repo is intentionally React + Vite/tsup + Storybook, a standalone component library, not a Next.js app"
  STATE_VOCABULARY: "100% — note: this package does not consume the shared Tailwind `src/styles/states.css` vocabulary; it ships its own self-contained, portable CSS custom-property tokens (--fc-*) so it works in any host regardless of that host's own token system, including one authored `:hover` rule in its own stylesheet — see end-of-turn report"
  STORYBOOK_SETUP: "100%"
  STORYBOOK_TESTING: "NOT REQUIRED — GAP, not an inapplicability: no *.stories.tsx exist yet for FlowChart/PathDrawer/FlowNode — flagged as an open backfill item in the end-of-turn report"
  STORY_FORMAT: "NOT REQUIRED — GAP, same reason as STORYBOOK_TESTING: no stories authored yet"
  TEST_STACK: "100%"
  TYPESCRIPT_HYGIENE: "100%"
  UNIT_COVERAGE: "100% — per-export test coverage verified by reading the test files; the numeric coverage percentage itself requires the verifier's quoted `pnpm test:coverage` run, not asserted here"
  UTC_TIMESTAMPS: "NOT REQUIRED — no timestamps/dates handled anywhere in this feature"
  VERIFIER_MODES: "NOT REQUIRED — governs the verifier agent's own mode catalog, not a feature artifact"
  VERIFY_MANUAL_STORIES: "NOT REQUIRED — GAP, not an inapplicability: no per-component Verify-Manual story or library-wide Verify-Manual master page exist yet — flagged as an open backfill item in the end-of-turn report"
  WRITING_ORDER: "100%"
  DECISION_LOG: "NOT REQUIRED — no user ruling made during this spec-authoring dispatch requiring a log entry"
  BROWSER_VALIDATION: "NOT REQUIRED — GAP, not an inapplicability: BROWSER_VALIDATION.md's own rule is 'no walk, no stamp' — no real-browser Chrome-extension walk has been run for this feature yet (that is Mode E / ui-walker-core's job); the manual/<flow>.md scripts are authored and ready to be walked, but the walk itself has not happened — flagged in the end-of-turn report"
  API_ENVELOPE: "NOT REQUIRED — no API/HTTP responses in this feature"
  API_FIRST: "NOT REQUIRED — no services/APIs shipped by this feature; it is a pure rendering client"
  REQUIREMENTS_CONTRACT: "NOT REQUIRED — this is a legacy-backfill spec pass over already-shipped code, not a fresh user-requirements negotiation"
  SELF_HARDENING: "NOT REQUIRED — no lessons-ledger/regression-corpus activity in this dispatch"
  CONTEXT_ECONOMY: "100%"
