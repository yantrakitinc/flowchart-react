feature: src/parse

# NOTE (spec-writer boundary, per feature-spec-writer-core.md "What you do NOT do" +
# LOCK_FILES.md stamping_authority: "only verifier Mode A writes last_validated"):
# this file is authored by the spec-writer pass as a reverse-engineered COMPLIANCE
# MAP for the verifier to check against — it is NOT a self-issued stamp. status
# ships "unlocked" here regardless of how green the map below looks; only verifier
# Mode A (after actually running pnpm typecheck / pnpm lint / pnpm test:coverage
# against this folder + its blast radius) may flip status to "locked", set
# verified: "100%", and write a real last_validated UTC timestamp.
#
# One entry below (TEST_NAMING) is a documented, ALREADY-FOUND gap, not a
# placeholder: the feature's only test file lives at
# src/parse/parseFlowchart.test.ts, not src/parse/__tests__/parseFlowchart.test.ts
# as TEST_NAMING#unit_layout requires. Per LOCK_FILES the per-standard verdict
# enum is only "100%" | "NOT REQUIRED" — neither is honest here, so this entry
# intentionally uses a third, non-enum value ("GAP — ...") to avoid stamping a
# false green. This will (correctly) fail scripts/verify/verify-standards-compliance
# until the file is relocated and the entry is corrected to "100%" by whoever
# re-runs verifier Mode A.

status: unlocked

standards:
  AUTHORIZATION_STANDARDS: "NOT REQUIRED — no DB, no repository, no permission surface; pure IR compiler"
  COMPONENT_CREATION: "NOT REQUIRED — no atomic UI primitive in this folder"
  COMPOSITE_CREATION: "NOT REQUIRED — no composite UI component in this folder"
  PAGE_CREATION: "NOT REQUIRED — no route/page/surface in this folder"
  E2E_TESTING: "NOT REQUIRED — no live HTTP/browser transport to drive; pure-function unit coverage (UNIT_COVERAGE, 100%) is the applicable test surface for a library compiler with no I/O"
  GOOGLE_FIREBASE_STANDARDS: "NOT REQUIRED — no Firebase usage anywhere in this feature"
  INDUSTRY_STANDARDS_STACK: "NOT REQUIRED — no HTTP/OAuth/OIDC/WebAuthn/etc. named-standard surface in a pure text↔IR compiler"
  PACKAGE_PROJECT_STANDARDS: "NOT REQUIRED — repo-level publish/versioning/layout concern (package.json, tsup, CONFIRMED=1 gate); not scoped to an individual feature folder"
  PROCESS_DISCIPLINE: "100% — scope followed (parse+serialize only, no anticipation), defects surfaced not silently patched (see spec.md Out of scope + serialize-flowchart.flow.yaml edge_boundary_invalid_*_type), foundational order respected (IR type contract in src/ir/ precedes this compiler)"
  PROJECT_ARCHITECTURE: "NOT REQUIRED — governs single Next.js app shape (SSR authorization, API routes); this repo is a published React component library, not a Next.js app"
  SITE_BLUEPRINT: "NOT REQUIRED — no site/pages in this repo shape"
  STANDARDS_CREATION_STANDARDS: "NOT REQUIRED — meta-standard for authoring standards docs; N/A to source code"
  SUPERPOWERS: "NOT REQUIRED — plugin/skill catalog; N/A to this feature's source/spec"
  TEST_NAMING: "GAP — src/parse/parseFlowchart.test.ts backs this feature at the feature root; TEST_NAMING#unit_layout requires <feature>/__tests__/<code>.test.ts (unit:parse/parseFlowchart). Relocate the test file to src/parse/__tests__/parseFlowchart.test.ts before this can legitimately read 100%. (Intentional non-enum value — see file header note.)"
  VERCEL_STANDARDS: "NOT REQUIRED — no Vercel deploy target for this feature (repo host_provider is cloudflare per README.yaml; this feature has no deploy surface at all)"
  ACCESSIBILITY: "NOT REQUIRED — no UI/DOM output"
  AGENT_AFFORDANCES: "NOT REQUIRED — no DOM/data-testid surface; the library's public API is documented via spec.yaml + manual/<flow>.md library playbooks instead"
  API_SURFACE: "NOT REQUIRED — no HTTP endpoint, no server action"
  BRANCHES_AND_COMMITS: "NOT REQUIRED — repo-level git workflow (branch naming, commit format), not a per-feature spec/code artifact"
  CODE_DOCUMENTATION: "100% — every export (parseFlowchart, iParseOptions, serializeFlowchart, FlowchartParseError) carries parent-facing JSDoc; no banned commented-out code or WHAT-comments found; SETUP markers used correctly in errors.ts/index.ts"
  COMPONENT_DOCS: "NOT REQUIRED — no UI component in this folder"
  COMPONENT_FOLDERS: "NOT REQUIRED — no UI component folder shape to check"
  COMPONENT_LIBRARY_DOCTRINE: "NOT REQUIRED — this folder is a pure IR compiler, not a design-system primitive"
  DESIGN_TOKENS: "NOT REQUIRED — no CSS/styling in this feature"
  ENV_VARS: "NOT REQUIRED — no environment variables read or required by this feature"
  FLOW_CONTRACT: "100% — flows/parse-flowchart.flow.yaml + flows/serialize-flowchart.flow.yaml enumerate happy + every declared error_* (12 for parse; 0 declared throws for serialize, documented instead as edge_boundary_invalid_*_type per KNOWN-NOT-VALIDATED) + every applicable edge_boundary_* risk class; no edge_concurrent_*/edge_partial_state_*/edge_authority_* apply (no shared state, single return, no auth) and their absence is a legitimate n/a for a pure synchronous function, not an omission; both flows ship a mermaid diagram (>= 2 paths)"
  USER_JOURNEYS: "NOT REQUIRED — library has no end-user UI journey; consumed by developer code via the public API documented in spec.yaml + manual/<flow>.md, not an app-level SaaS journey"
  LABS: "NOT REQUIRED — no /labs UI sandbox surface; no UI at all"
  HEXAGONAL_ARCHITECTURE: "NOT REQUIRED — no service/repository/adapter/DB layers exist in this feature to judge against the hexagonal call graph; a pure function with zero external dependencies has nothing to inject"
  I18N: "n/a — FlowchartParseError.reason strings are developer-facing DSL diagnostics for the DSL author, not end-user UI copy; not routed through an i18n layer (see spec.yaml cross_cutting.i18n) — NOT REQUIRED"
  ISSUES: "NOT REQUIRED — repo-level GitHub issue workflow, not a per-feature spec/code artifact"
  LOCAL_DEV_NETWORK: "NOT REQUIRED — no local dev server/port in this feature"
  LOCK_FILES: "100% — this file follows the standards-compliance.yaml schema (feature path, status, standards map keyed to every INDEX.yaml entry); status intentionally ships unlocked pending verifier Mode A per stamping_authority (spec-writer never self-stamps)"
  LOGGING: "NOT REQUIRED — no logger call anywhere in this feature; failures are typed throws (FlowchartParseError), not logged"
  MANUAL_FLOWS: "100% — manual/parse-flowchart.md + manual/serialize-flowchart.md are spec-derived, code-blind-authored, adversarial (malformed DSL / type-system-bypass / empty-graph / data-leak steps), self-contained, five-section-shaped, with an explicit machine_result ok boolean; Target/Report sections are adapted for a pure-library surface with no running server (documented adaptation, see each file's Target + Report notes) rather than silently reusing the web-app POST route this repo does not have"
  MOBILE_FIRST: "NOT REQUIRED — no UI rendering, no viewport"
  MOCKING_DOCTRINE: "NOT REQUIRED — zero external dependencies (no Date.now/fetch/Math.random/process.env/fs) to inject or mock; tests call the pure functions directly"
  NAMING: "100% — iParseOptions/iNodeAcc/iConsumedNode/iConsumedLink/iFlowGraph/iFlowNode/iFlowEdge/iDirection/iEdgeType/iNodeType all carry the i-prefix; FlowchartParseError carries no Class suffix by design (it IS the public error type name, matching the Error subclassing convention, not an internal service class); files are camelCase (parseFlowchart.ts, serializeFlowchart.ts, errors.ts, index.ts)"
  NO_THEATER_TESTS: "100% — existing test suite (src/parse/parseFlowchart.test.ts) asserts real return shapes/thrown-error identity per case; no snapshot-only or assertion-free tests found"
  PULL_REQUESTS: "NOT REQUIRED — repo-level PR lifecycle (six-H2 body shape, merge/close), not a per-feature spec/code artifact"
  README_CONTRACT: "NOT REQUIRED — repo-level README.yaml/README.md already covers the whole repo; not a per-feature artifact"
  REPO_GATE_INSTALLATION: "NOT REQUIRED — repo-level hook/CI mechanics, not a per-feature artifact"
  REPO_PROVISIONING: "NOT REQUIRED — repo/board/host provisioning already done at repo level, not a per-feature concern"
  ROOT_LAYOUT: "NOT REQUIRED — repo-level /docs + /code root folder contract, not a per-feature artifact"
  SCENARIO_ENUMERATION: "100% — flows walk the actual code path (not the request): every distinct throw site in parseFlowchart.ts is its own error_* path (12), plus boundary scenarios (comments/blanks, chains, redeclaration, cycle fallback, tight tokenization); serialize's lack of runtime type validation is surfaced as edge_boundary_invalid_*_type (KNOWN-NOT-VALIDATED with an explicit probability/acceptability note), not silently left uncovered; concurrency/partial-state/authorization/idempotency/time-based categories are legitimately n/a for a synchronous, stateless, unauthenticated pure function"
  SOURCE_COVERAGE: "100% — every .ts file under src/parse/ (parseFlowchart.ts, serializeFlowchart.ts, errors.ts, index.ts) resolves to this __specs__/spec.yaml (feature_name: parse, unique repo-wide, first top-level field)"
  SOURCE_FOLDERS: "100% — src/parse/ is a single self-contained per-feature folder with no padding subfolders; one operation (the DSL⇄IR compiler, parse+serialize as its two directions) per SOURCE_COVERAGE#one_operation_per_feature"
  SPEC_CONTRACT: "100% — spec.yaml is implementation-contract-only (no narrative), spec.md is narrative-only (Concept/Files/Out of scope), all four cross_cutting keys declared (wcag/auth/mobile/i18n, each 'n/a — <reason>'), links.flows/links.tests/links.manual all populated with real existing paths"
  STACK: "NOT REQUIRED — repo-level framework/tooling baseline decision (React library, not Next.js), already documented in README.yaml/package.json; not a per-feature contract"
  STATE_VOCABULARY: "NOT REQUIRED — no UI state classes in this feature"
  STORYBOOK_SETUP: "NOT REQUIRED — no UI component to host in Storybook"
  STORYBOOK_TESTING: "NOT REQUIRED — no UI component two-way coverage applicable"
  STORY_FORMAT: "NOT REQUIRED — no Storybook stories in this feature"
  TEST_STACK: "100% — Vitest is the sole test tool used (unit_integration), correctly so (no fetch/DB/browser involved, so MSW/pglite/Playwright/Puppeteer/Swagger-UI are all legitimately unused); permission_aware_tests n/a (no service/repository/route touched); mock_seed n/a (pure function needs no fixture seed)"
  TYPESCRIPT_HYGIENE: "100% — strict-mode-compatible source (no `any`, no @ts-ignore/@ts-expect-error found); types co-located with the runtime code that owns them (iParseOptions in parseFlowchart.ts, iNodeAcc/iConsumedNode/iConsumedLink kept private/unexported since no other file imports them); no banned <name>.types.ts or per-folder types.ts"
  UNIT_COVERAGE: "100% — every exported behavior (parseFlowchart, serializeFlowchart) has extensive unit coverage in the existing test file: happy path, every declared error path (SCENARIO_ENUMERATION-walked, matches flow.yaml throws), every distinct edge case (flow.yaml edge_boundary_* — comments, chains, redeclaration, cycle fallback, tight tokenization, round-trip stability)"
  UTC_TIMESTAMPS: "NOT REQUIRED — no timestamps anywhere in this feature"
  VERIFIER_MODES: "NOT REQUIRED — defines the verifier's own operational mode catalog (agent-side process); not a per-feature build/spec deliverable this feature produces. This feature IS a candidate for Mode A verification under that catalog."
  VERIFY_MANUAL_STORIES: "NOT REQUIRED — no Storybook component, so no per-component Verify-Manual story or master page applies"
  WRITING_ORDER: "100% — three-phase order followed for this backfill: spec.yaml + spec.md + flows/ + manual/ authored here (spec_phase) BEFORE any source change (none made — source pre-existed and was read-only per this pass); coder_phase and verify_phase remain to run next (coder to relocate the test file per the TEST_NAMING gap; verifier Mode A to stamp this file locked on green)"
  DECISION_LOG: "NOT REQUIRED — no ambiguous user-facing ruling arose during this backfill that required logging; nothing to log"
  BROWSER_VALIDATION: "NOT REQUIRED — no browser/UI surface in this feature; manual/<flow>.md playbooks here are library-import playbooks, not browser walks"
  API_ENVELOPE: "NOT REQUIRED — no HTTP API/JSend envelope in this feature"
  API_FIRST: "NOT REQUIRED — no HTTP API to ship before a UI; pure library export"
  REQUIREMENTS_CONTRACT: "NOT REQUIRED — governs the pre-build requirements-lock ceremony for net-new autonomous building; this is a legacy backfill of already-shipped, already-tested behavior, not net-new scope"
  SELF_HARDENING: "NOT REQUIRED — repo-wide learning-loop process (lessons ledger, regression corpus), not a per-feature spec/code artifact"
  CONTEXT_ECONOMY: "NOT REQUIRED — governs agent context/dispatch economy (this very dispatch), not a per-feature source/spec deliverable"
