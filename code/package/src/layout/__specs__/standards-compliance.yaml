# standards-compliance.yaml — per-feature lock + freshness marker.
# See LOCK_FILES.yaml for schema + workflow rules.
# Lives at: code/package/src/layout/__specs__/standards-compliance.yaml
#
# status is UNLOCKED by design at spec-writer hand-off: per WRITING_ORDER.yaml +
# AGENT_ARCHITECTURE.md, ONLY verifier Mode A writes status:locked / verified:100% /
# last_validated (a mechanical typecheck+lint+coverage+freshness run). The
# feature-spec-writer role never stamps this file's overall lock, regardless of how
# strong the circumstantial evidence looks from reading the source. Per-standard
# verdicts below reflect direct code inspection performed while authoring this spec
# (source + existing tests were read in full) — dispatch verifier Mode A to convert
# this scaffold into a real 100%-verified, locked stamp.

status: unlocked
verified: "0%"
last_validated: ""
feature: code/package/src/layout

standards:
  ACCESSIBILITY: NOT REQUIRED   # no DOM/UI rendered by this feature (pure geometry computation; src/react owns the rendered surface)
  AGENT_AFFORDANCES: NOT REQUIRED   # no HTTP/UI surface; no data-testid/data-agent-* attrs applicable to a pure library function
  API_ENVELOPE: NOT REQUIRED   # no HTTP response envelope; no HTTP surface at all
  API_FIRST: NOT REQUIRED   # no API/UI surface; consumed in-process by the published package's own React layer or a host app
  API_SURFACE: NOT REQUIRED   # no HTTP route
  AUTHORIZATION_STANDARDS: NOT REQUIRED   # no DB access, no principal, no permission-gated operation
  BRANCHES_AND_COMMITS: NOT REQUIRED   # repo-wide git workflow rule, not a per-feature code contract
  BROWSER_VALIDATION: NOT REQUIRED   # no UI surface to browser-walk
  CODE_DOCUMENTATION: "100%"   # every export (layout, dagreEngine, elkEngine, loadElk, every type in types.ts) carries parent-facing JSDoc; verified by direct read of the source
  COMPONENT_CREATION: NOT REQUIRED   # no atomic UI component
  COMPONENT_DOCS: NOT REQUIRED   # no UI component
  COMPONENT_FOLDERS: NOT REQUIRED   # no component folder
  COMPONENT_LIBRARY_DOCTRINE: NOT REQUIRED   # not a design-system primitive
  COMPOSITE_CREATION: NOT REQUIRED   # no composite UI
  CONTEXT_ECONOMY: NOT REQUIRED   # agent-dispatch/session-budget discipline, not a per-feature code contract
  DECISION_LOG: NOT REQUIRED   # no user ruling specific to this feature's design was made during this backfill
  DESIGN_TOKENS: NOT REQUIRED   # no styling/CSS tokens
  E2E_TESTING: NOT REQUIRED   # no HTTP/UI surface of its own to drive end-to-end; exercised in-process, and via the consuming src/react layer's own E2E where applicable
  ENV_VARS: NOT REQUIRED   # no environment variables read
  FLOW_CONTRACT: "100%"   # flows/*.flow.yaml authored for every exported behavior (layout, dagreEngine.run, elkEngine.run, loadElk) with exhaustive happy/error_*/edge_* paths + mermaid
  GOOGLE_FIREBASE_STANDARDS: NOT REQUIRED   # no Firebase usage
  HEXAGONAL_ARCHITECTURE: NOT REQUIRED   # this standard describes Next.js service/repository/DB call-graph layering; this feature has no DB/service/repository stack (the iLayoutEngine strategy + loadElk DI seam are analogous in spirit but not the described architecture)
  I18N: NOT REQUIRED   # no user-facing strings authored here; node/edge labels pass through unchanged from the caller-supplied IR
  INDUSTRY_STANDARDS_STACK: NOT REQUIRED   # no named industry protocol (OAuth/OIDC/etc.) implemented here
  ISSUES: NOT REQUIRED   # GitHub issue process, not a per-feature code contract
  LABS: NOT REQUIRED   # no /labs visual sandbox surface for an internal computation module
  LOCAL_DEV_NETWORK: NOT REQUIRED   # no dev server / port binding
  LOCK_FILES: "100%"   # this file itself conforms to the LOCK_FILES.yaml schema (required scalars + full standards: map, no padding fields)
  LOGGING: NOT REQUIRED   # no logging call sites
  MANUAL_FLOWS: NOT REQUIRED   # layout() has no HTTP/CLI/UI surface (pure in-process library call) — the standard's own applicability gate (HTTP/CLI/UI surface) is not met; manual/layout.md is nonetheless authored as an adapted package-consumer smoke playbook (see its Adaptation note)
  MOBILE_FIRST: NOT REQUIRED   # no UI viewport concerns; outputs numeric geometry only
  MOCKING_DOCTRINE: "100%"   # loadElk's `importer` param is DI for code-I-own (confirmed in elkEngine.ts); engines.test.ts injects a fake importer/engine rather than vi.mock("module")
  NAMING: "100%"   # every interface/type in this folder is i-prefixed (iLayoutEngine, iLayoutOptions, iPositionedGraph, iPositionedNode, iRenderEdge, iEngineContext, iPositions); no violations found
  NO_THEATER_TESTS: "100%"   # layout.test.ts/engines.test.ts assert real computed values (positions, direction ordering, dangling-edge filtering, thrown error messages) — not placeholder/theater assertions
  PACKAGE_PROJECT_STANDARDS: NOT REQUIRED   # repo-level packaging/publish concern (package/ layout, versioning, CONFIRMED=1 prepublish gate); not a per-feature contract
  PAGE_CREATION: NOT REQUIRED   # no page/route/surface
  PROCESS_DISCIPLINE: NOT REQUIRED   # process discipline for how work is done, not a codified per-feature artifact
  PROJECT_ARCHITECTURE: NOT REQUIRED   # describes a Next.js single-app architecture; this repo is a published React library with no such app shell
  PULL_REQUESTS: NOT REQUIRED   # PR lifecycle process, not a per-feature code contract
  README_CONTRACT: NOT REQUIRED   # repo-level README/README.yaml contract, not per-feature
  REPO_GATE_INSTALLATION: NOT REQUIRED   # repo-level hook/CI installation, not per-feature
  REPO_PROVISIONING: NOT REQUIRED   # repo/board provisioning, not per-feature
  REQUIREMENTS_CONTRACT: NOT REQUIRED   # pre-build requirements-lock ceremony; this is a backfill of already-shipped code, not a fresh autonomous build
  ROOT_LAYOUT: NOT REQUIRED   # repo-root folder contract, not per-feature
  SCENARIO_ENUMERATION: NOT REQUIRED   # the 11-category scenario walk is framed for request/service operations with DB/auth scenarios; this module's exhaustive paths are captured directly via FLOW_CONTRACT's own path enumeration
  SELF_HARDENING: NOT REQUIRED   # learning-loop/lessons-ledger process, not a per-feature code contract
  SITE_BLUEPRINT: NOT REQUIRED   # no site/page-tier surface
  SOURCE_COVERAGE: "100%"   # src/layout now resolves to __specs__/spec.yaml with feature_name as its first field, satisfying the walk-up rule
  SOURCE_FOLDERS: "100%"   # src/layout is a legitimate self-contained feature folder (types.ts/layout.ts/dagreEngine.ts/elkEngine.ts/index.ts); no padding folders
  SPEC_CONTRACT: "100%"   # this __specs__/ folder follows the required folder layout (spec.yaml, spec.md, flows/, manual/, standards-compliance.yaml) with all four cross_cutting keys declared
  STACK: NOT REQUIRED   # default framework/tooling baseline is a repo-wide decision (React/TypeScript/Vitest), not this single feature's contract
  STANDARDS_CREATION_STANDARDS: NOT REQUIRED   # meta-rule for writing standards docs, not features
  STATE_VOCABULARY: NOT REQUIRED   # no UI state classes
  STORYBOOK_SETUP: NOT REQUIRED   # no Storybook surface for this internal module
  STORYBOOK_TESTING: NOT REQUIRED   # no component to story-test
  STORY_FORMAT: NOT REQUIRED   # no story file
  SUPERPOWERS: NOT REQUIRED   # plugin/skill workflow concern, not a per-feature contract
  TEST_NAMING: "100%"   # vitest.config.ts explicitly globs `src/**/*.test.{ts,tsx}` — a documented, repo-wide, deliberate co-located test convention (every feature: ir/parse/paths/react/layout) rather than the __tests__/ subfolder shape; the path-derived unit:<feature-path>/<file> ID still resolves cleanly under this convention. Flagged to the user as a naming-convention reconciliation item (see this dispatch's end-of-turn report) rather than silently accepted or silently failed.
  TEST_STACK: NOT REQUIRED   # tool selection (Vitest) is the repo-wide TEST_STACK.yaml default; this feature introduces no new testing surface (no MSW/pglite/Playwright choice to make)
  TYPESCRIPT_HYGIENE: "100%"   # strict TS throughout; no `any`, no @ts-ignore/@ts-expect-error anywhere in this folder's source (verified by direct read)
  UNIT_COVERAGE: "100%"   # every export has tests exercising happy + error + edge paths (layout.test.ts, engines.test.ts); vitest.config.ts enforces perFile 100% thresholds repo-wide and this package ships (README.yaml status:shipping) under that gate — NOT personally re-run this turn; verifier Mode A should confirm the mechanical number
  UTC_TIMESTAMPS: NOT REQUIRED   # no timestamps handled by this feature
  VERCEL_STANDARDS: NOT REQUIRED   # no Vercel deploy for this npm package
  VERIFIER_MODES: NOT REQUIRED   # describes the verifier's own mode catalog, not a per-feature contract
  VERIFY_MANUAL_STORIES: NOT REQUIRED   # no Storybook Verify-Manual surface (no UI component here)
  WRITING_ORDER: "100%"   # spec.yaml + spec.md + flows/ authored before any code change in this dispatch (no code was modified — pure backfill of an already-shipped feature)
