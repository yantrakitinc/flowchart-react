flow: flowDocCandidates
kind: helper
source: scripts/verify-flow-coverage/verify-flow-coverage.mjs
symbol: flowDocCandidates
inputs:
  behaviorName: exported function or method identifier
returns:
  - "string[] — every accepted flow-doc filename (kebab forms × accepted extensions)"
throws: []
calls:
  - toKebabCandidates
called_by:
  - audit
emits_events: []
side_effects_on_success: ["none"]
side_effects_on_failure: "none"
transaction: none
test: scripts/verify-flow-coverage/__tests__/verify-flow-coverage.test.mjs
spec: scripts/verify-flow-coverage/__specs__/spec.yaml
ai_agent_action:
  when_to_call: "internal — only audit calls this"
  when_not_to_call: "external callers"
  natural_language_examples: []
  agent_invocation: "internal — not callable"
  confirm_with_user_before: "none — read-only"
  summarize_to_user_after: "n/a — internal helper"
paths:
  happy:
    - kebab-case the behavior name (with any acronym-collapsed alternates)
    - for each kebab form × each accepted extension ('.flow.yaml' | '.flow.md'), append a candidate
    - return the full list
  edge_acronym:
    - behavior name like 'registerOpenAPI' → both 'register-open-api.flow.yaml' and 'register-openapi.flow.yaml' (and .md variants) appear
