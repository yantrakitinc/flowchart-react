flow: classifyFileStray
kind: predicate
source: scripts/verify-no-stray-scaffolding/verify-no-stray-scaffolding.mjs
symbol: classifyFileStray
inputs:
  file: absolute file path
returns:
  - "{ kind, detail } when file is a stray shape"
  - "null when file is fine"
throws: []
calls: []
called_by:
  - audit
emits_events: []
side_effects_on_success: ["none"]
side_effects_on_failure: "none"
transaction: none
test: scripts/verify-no-stray-scaffolding/__tests__/verify-no-stray-scaffolding.test.mjs
spec: scripts/verify-no-stray-scaffolding/__specs__/spec.yaml
ai_agent_action:
  when_to_call: "internal — only audit calls this"
  when_not_to_call: "external callers"
  natural_language_examples: []
  agent_invocation: "internal — not callable"
  confirm_with_user_before: "none — read-only"
  summarize_to_user_after: "n/a — internal helper"
paths:
  happy:
    - file is a normal source file; return null
  error_flows_md:
    - basename is 'FLOWS.md'
    - return { kind: 'flows-md', detail: 'no FLOWS.md anywhere — AI agent crawls __specs__/' }
  error_code_confidence_outside_specs:
    - basename is 'CODE_CONFIDENCE.md' and path does NOT contain '/__specs__/'
    - return { kind: 'code-confidence-outside-specs', detail: 'CODE_CONFIDENCE.md must live inside __specs__/' }
  edge_code_confidence_inside_specs:
    - basename is 'CODE_CONFIDENCE.md' and path contains '/__specs__/'
    - return null (legitimate placement)
