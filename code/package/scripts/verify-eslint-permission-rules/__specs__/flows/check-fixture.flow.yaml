flow: checkFixture
kind: predicate
source: scripts/verify-eslint-permission-rules/verify-eslint-permission-rules.mjs
symbol: checkFixture
inputs:
  fixture: "{ name, virtualPath, source, expect, ruleId, messageFragment }"
  ports: "{ eslintBin, cwd, runEslint? }"
returns:
  - "{ ok: true }"
  - "{ ok: false, error }"
throws: []
calls:
  - runEslintOnSnippet
called_by:
  - audit
emits_events: []
side_effects_on_success: ["none"]
side_effects_on_failure: "none"
transaction: none
test: scripts/verify-eslint-permission-rules/__tests__/verify-eslint-permission-rules.test.mjs
spec: scripts/verify-eslint-permission-rules/__specs__/spec.yaml
ai_agent_action:
  when_to_call: "internal — only audit calls this"
  when_not_to_call: "external callers"
  natural_language_examples: []
  agent_invocation: "internal — not callable"
  confirm_with_user_before: "none — read-only"
  summarize_to_user_after: "n/a — internal helper"
paths:
  violation_satisfied:
    - expect='violation' AND ≥ 1 matching diagnostic; ok=true
  violation_missing:
    - expect='violation' AND no matching diagnostic; ok=false with 'expected ... to fire'
  allowed_satisfied:
    - expect='allowed' AND zero matching diagnostics; ok=true
  allowed_but_fired:
    - expect='allowed' AND ≥ 1 matching diagnostic; ok=false with 'expected ... to NOT fire'
  edge_no_file_result:
    - ESLint returned zero file results (malformed JSON / empty array); ok=false
