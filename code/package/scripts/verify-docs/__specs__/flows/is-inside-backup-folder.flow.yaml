flow: isInsideBackupFolder
kind: predicate
source: scripts/verify-docs/verify-docs.mjs
symbol: isInsideBackupFolder
inputs:
  path: absolute path (file or directory)
returns:
  - "true when any path segment equals '__specs__.backup'"
  - "false otherwise"
throws: []
calls: []
called_by:
  - audit
emits_events: []
side_effects_on_success: ["none"]
side_effects_on_failure: "none"
transaction: none
test: scripts/verify-docs/__tests__/verify-docs.test.mjs
spec: scripts/verify-docs/__specs__/spec.yaml
ai_agent_action:
  when_to_call: "internal — only audit calls this"
  when_not_to_call: "external callers"
  natural_language_examples: []
  agent_invocation: "internal — not callable"
  confirm_with_user_before: "none — read-only"
  summarize_to_user_after: "n/a — internal helper"
paths:
  happy_inside_backup:
    - one segment equals '__specs__.backup' → return true
  edge_not_backup:
    - no segment equals '__specs__.backup' → return false
  edge_segment_match_not_substring:
    - path contains '__specs__.backupX' → return false (segment match, not substring)
