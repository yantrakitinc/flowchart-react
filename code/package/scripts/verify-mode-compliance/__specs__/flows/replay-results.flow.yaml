flow: replayResults
kind: pure-helper
source: scripts/verify-mode-compliance/verify-mode-compliance.mjs
symbol: replayResults
inputs:
  results: "ReadonlyArray<{ entry, status, stdout, stderr, ms }>"
  write: "(s: string) => void"
  writeErr: "(s: string) => void"
returns:
  - "void"
throws: []
calls:
  - write
  - writeErr
called_by:
  - main (after each phase)
emits_events: []
side_effects_on_success: ["forwards each result's stdout to `write` then its stderr to `writeErr` — no interleaving"]
side_effects_on_failure: "n/a"
transaction: none
test: scripts/verify-mode-compliance/__tests__/verify-mode-compliance.test.mjs
spec: scripts/verify-mode-compliance/__specs__/spec.yaml
ai_agent_action:
  when_to_call: "internal — only main composes replayResults"
  when_not_to_call: "external callers"
  natural_language_examples: []
  agent_invocation: "internal — not callable"
  confirm_with_user_before: "none"
  summarize_to_user_after: "n/a — internal helper"
paths:
  happy:
    - for each result, emit stdout (if non-empty) then stderr (if non-empty)
    - results are walked in input order; each is fully written before the next begins
  edge_empty_streams:
    - both stdout and stderr empty → nothing written
