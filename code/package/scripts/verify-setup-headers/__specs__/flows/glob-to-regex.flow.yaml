flow: globToRegex
kind: pure-helper
source: scripts/verify-setup-headers/verify-setup-headers.mjs
symbol: globToRegex
inputs:
  glob: shell-style glob pattern (anchored at src/)
returns:
  - "RegExp object matching the glob's expansion"
throws: []
calls: []
called_by:
  - SETUP_REGEXES (module-load) → classify (via audit)
emits_events: []
side_effects_on_success: ["none"]
side_effects_on_failure: "none"
transaction: none
test: scripts/verify-setup-headers/__tests__/verify-setup-headers.test.mjs
spec: scripts/verify-setup-headers/__specs__/spec.yaml
ai_agent_action:
  when_to_call: "internal — compiled at module load, never called at runtime"
  when_not_to_call: "external callers"
  natural_language_examples: []
  agent_invocation: "internal — not callable"
  confirm_with_user_before: "none — pure function"
  summarize_to_user_after: "n/a — internal helper"
paths:
  literal_segments:
    - input contains only literal path segments and `/`
    - output is the escaped literal regex
  single_star:
    - input contains `*` (one asterisk)
    - output substitutes `[^/]*`
  doublestar:
    - input contains bare `**`
    - output substitutes `.*`
  doublestar_slash:
    - input contains `**/`
    - output substitutes `(?:.*/)?` so the empty-prefix case matches too
  regex_metacharacters:
    - input contains `.` or `+` or other regex metacharacters
    - characters are escaped with a backslash so they match literally
